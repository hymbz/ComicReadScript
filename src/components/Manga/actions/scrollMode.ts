// import { clamp } from 'helper';

// import { _setState, refs, setState, store } from '../store';

// import { abreastScrollWidth } from './abreastScroll';
// // import { scrollLength, scrollPercentage } from './scrollbar';

// // /** 并排卷轴模式下滚动到指定位置 */
// // export const abreastScrollBy = (x: number, to = false) => {
// //   setState((state) => {
// //     state.page.offset.x.px = clamp(0, x, abreastScrollWidth());
// //   });
// // };
